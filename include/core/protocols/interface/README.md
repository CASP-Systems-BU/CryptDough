The `protocols/interface` directory contains the MPC protocol interface required by cdough.

Contents:

- `protocol_base.h` – Non-template base protocol interface.
- `protocol.h` - Base protocol interface.
- `protocol_factory.h` – Factory that instantiates protocol objects.