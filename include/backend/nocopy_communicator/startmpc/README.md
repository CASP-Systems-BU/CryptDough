The `startmpc/` directory contains the launcher used to bring up multi-party execution over
sockets.

Contents:

- `startmpc` – Launcher script that starts the MPC parties.
- `startmpc.h` – Connection-setup helpers (outgoing/listening sockets) used to establish the party-to-party network.
