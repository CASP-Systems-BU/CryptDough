The `null_communicator/` directory sets up the null (single-process) service used for testing and
local execution without inter-party networking.

Contents:

- `setup.h` – Runtime and randomness setup for the null service.
